# Cookies vs Sessions

- Cookies: stored on client, sent with requests, used for state and tracking.
- Sessions: stored server-side, referenced by session id in a cookie.
- Security considerations: HttpOnly, Secure, SameSite flags.
