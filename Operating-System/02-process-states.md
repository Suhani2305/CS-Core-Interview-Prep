# Process States

- Common states: New, Ready, Running, Waiting (Blocked), Terminated.
- State transitions: scheduling, I/O wait, interrupt handling.
- Process control block (PCB) stores process context.
